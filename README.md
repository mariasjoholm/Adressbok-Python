# Adressbok-Python
A contact management application built with Python and Streamlit. The application allows users to create, edit, delete, search, and store contacts using JSON-based persistent storage.
